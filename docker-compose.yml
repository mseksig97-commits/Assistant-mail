version: "3.9"

services:
  mail-agent:
    build: .
    image: assistant-mail
    container_name: assistant-mail
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./config:/app/config
      - ./logs:/app/logs
